local   all             all                                     trust

# Restricted to the automation-network compose subnet (see docker-compose.yml);
# postgres is never published to the host, so this only ever sees traffic
# from containers on that network.
host    all             all             172.30.0.0/24           md5
